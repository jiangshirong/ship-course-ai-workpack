# 2026-09-25 复用修订

## 同日v7：把 AutoCAD 改为最终 DWG 验收层

重新划分无 AutoCAD 的 DXF 开发层、DWG 转换层和原生 DWG 验收层。README、AGENTS、完整流程、检查清单、交付规范和环境说明不再把 AutoCAD/MCP 写成全部开发工作的硬性前置；仍明确真实 DWG 需要经过已验证的 DWG 保存和回读。v7未修改个人成品。

## 同日v6：补充 AutoCAD 与 CAD MCP 前置条件

README、AGENTS、环境说明和发布说明现在明确：真实 DWG 需要可运行的 AutoCAD；CAD MCP 是推荐适配器而非唯一指定仓库；无 AutoCAD 时只能交付 DXF 中间结果或由用户手动另存 DWG。补充了 AutoCAD ActiveX `SaveAs` 官方链接和 MCP 预检顺序。v6未修改个人成品。

## 同日v5：补全对话中的格式细节

补充教师明确采用参数表显示值时的精度规则，以及 C 型长船双层底桁架加强、人孔可通行和使用高度要求；同步更新 T/C 分支入口和检查清单。v5仍未修改个人成品。

## 同日v4：整合两个作业对话的最终格式规范

新增 `docs/交付格式与船型制图规范.md`，把两个对话中反复确认的交付要求写成入口规则：最终只交原文俄文姓名的 DWG 和一份俄语 XLSX，不加 RU、不交中文表，不把 DXF/DWL/DWL2 或过程性 AI 文字放进成品；CAD按教师示例完成五类基本视图，并禁止白色背景板和悬空/多余构件。补充 T 型油船的内舷侧、中纵舱壁 0.5 份、50 mm 间距、内底厚度和示例加强筋要求；补充 C 型纵骨架/全横骨架分支及下层甲板横梁—加强纵桁的区别。

新增 `scripts/check_delivery.py`，可在关闭 AutoCAD 后检查输出目录是否恰好包含两个交付文件；回归测试增加交付目录反例。README、AGENTS、完整流程、模板入口、教师反馈、骨架依据、检查清单和工具说明均已链接或落实该规范。v4没有修改任何个人作业文件。

## 同日v3：第二次实际试用后

发现主表12组通过却漏掉4组附加立柱、满载/压载外压混用、未经证实的有利减载和扩表遮挡。v3将选型与实际表内检查扩到16组，并补充CL身份绑定；新增最终XLSX缓存验收、压力配对校验、schema3显式假设与字段说明、5组已知答案和完整组合截面推导。几何闭合、固定拓扑及已知缺陷前置到入口文档；AutoCAD增加显式IDispatch访问及排障路径。

验证：11组辅助回归通过（含原6475条历史公式回归）；5组已知答案通过；合成配置完成生成、重算、16组选型和实际表检查，两轮稳定；实际试用成品2832条公式缓存逐条匹配，但构件检查明确拦下CL-F1～4。真实AutoCAD读取1376实体、56层、65尺寸，并成功另存独立转换测试DWG。个人成品未改。

这不是新船端到端工程认证，未执行Excel内复算或全部规范工况验证。以下保留v2修订时的历史记录。

## v2记录

修订触发于一次实际新船试用：目录输入歧义、无办公软件的重算困难、板带残余宽度、选型后等效梁仍引用旧型号、转换脚本失败，以及反馈本身存在坐标判断错误。

已完成：

- 输入说明与历史成果分开；公开包有输入说明但不带个人数据。
- 限定公式计算器、全部单元格重算报告，以及新XLSX的数值/文字/布尔缓存写入。公式错误中止，不复制旧缓存。
- T型工作副本参数化：各段板带闭合、正确的内舷侧梁拱坐标、分开的甲板压力包络、型材性质到等效梁的关联。保留历史底稿用于追溯。
- 几何检查器与全目录HP候选枚举，明确检查边界与最轻声明的约束范围。
- AutoCAD返回文档使用dynamic dispatch并显式绑定SaveAs，拒绝覆盖或保存用户其他活动图纸。
- 修订流程、AI约定、检查清单及许可来源说明。

实际验证：

- Windows、本机Python 3.12.14；8组回归测试通过，含多项错误反例、缓存往返、独立矩形截面积分、模型关联及3组主尺度变化下的板带闭合。
- T原底稿2876、T后续修正2915、C纵骨架684个有历史缓存的公式完成回归对照，数值容差为相对/绝对1e-9。此项证明子集实现复现历史算术，不能证明原公式正确。
- 对实际试用成品的2875条公式执行修订引擎计算；未把试用成品改写为修正版。
- 另外在隔离测试副本中完成这2875条公式的新缓存写入与读取对照，确认原公式未改变、缓存均可读取。合成示例命令链完成几何检查→草稿生成→2832条公式重算。测试副本不作为正确作业交付。
- 本机AutoCAD COM返回版本`25.1s (LMS Tech)`；打开实际成品副本检查实体、尺寸、层及关键坐标，并通过修订转换脚本将试用DXF另存为独立测试DWG。

没有完成：Excel/LibreOffice内重算及XLSX视觉验收、所有新船型端到端生成、全部规范工况/屈曲/强支承构件审查、最终DWG的完整工程验收。几何工具检查声明数据，不自动审查任意DWG。T型草稿保留固定拓扑，其余荷载/板厚/支承仍需设计者审查。

个人成品、原始输入、详细审查证据和测试中间文件留在本地inputs/work/outputs，不随公开包分发。本次未创建远程GitHub仓库或推送。
